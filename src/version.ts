// Bump this on each delivered build so the running app shows which version it
// is. If the badge in the header doesn't match the latest zip, you're looking
// at an older/cached build that hasn't been deployed yet.
export const APP_VERSION = '0.13.0';
